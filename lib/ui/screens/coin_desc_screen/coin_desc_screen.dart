import 'package:flutter/material.dart';

class CoinDescScreen extends StatelessWidget {
  const CoinDescScreen({Key? key}) : super(key: key);

  @override
  Widget build(BuildContext context) {
    return Container();
  }
}
